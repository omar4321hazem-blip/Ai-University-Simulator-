# Ai-University-Simulator-
mkdir backend frontend echo "# Backend" > backend/README.md echo "# Frontend" > frontend/README.md git add . git commit -m "Initial commit: Project structure" git push -u origin main
